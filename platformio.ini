[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; NVS (Preferences) is used for storage — no EEPROM lib needed
; NVS lives in its own dedicated flash partition and survives firmware uploads
lib_deps =
